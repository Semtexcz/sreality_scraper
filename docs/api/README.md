# API Docs

This directory is reserved for generated HTML API documentation.

At the moment, the previously generated files are not part of the current working tree snapshot. Regenerate them here if API docs are needed for the refactored module layout.
